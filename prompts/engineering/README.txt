simple: prompt without insertion of mismatches

addition: prompt with insertion of additional features in self description

missing: prompt with insertion of missing features in self description

split: prompt with insertion of split features

collapsed: prompt with insertion of collapsed features

mixed: prompt with insertion of multiple (different) mismatches
